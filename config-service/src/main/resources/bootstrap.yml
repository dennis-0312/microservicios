server:
  port: 8081

spring:
  cloud:
    config:
      server:
        composite:
          type: git
          uri: https://github.com/dennis-0312/microservicios.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t